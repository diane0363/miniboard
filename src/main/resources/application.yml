spring:
  profiles:
    active: dev
  config:
    import: optional:application-secret.yml   # ★ secret 파일을 끌어온다
  application:
    name: miniboard
  mvc:
    hiddenmethod:
      filter:
        enabled: true        # ★ HTML form의 PUT/DELETE 우회 활성화 (기본값 false!)

